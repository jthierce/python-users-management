from models.user import User
from models.display import Display

Display.welcome()
login = input("Login:\n")
print(password_encrypted)
while(true):
    user = User.login_super_admin()
    if user is None:
        print(f"Don't find the superadmin with the username: #{username}")
want_exit = False
while()